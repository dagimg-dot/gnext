# GNOME Extension Template

This directory will contain the template for new GNOME extensions.

The template will be copied when users run:

```bash
bun create gnext@latest my-extension
```

**Note:** Template content will be added later.

